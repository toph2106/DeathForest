using UnityEngine;

/// <summary>
/// Helper MonoBehaviour dùng để chạy các Coroutine chuyển cảnh (Fade) khi GameObject chính bị ẩn (SetActive(false))
/// </summary>
public class FadeCoroutineRunner : MonoBehaviour
{
}
